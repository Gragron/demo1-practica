import React from "react";

const Image = (props) =>  
        <img 
            src={props.img} 
            alt="" 
        />;

// const Image = (props) => <p> {props.img} </p>

export default Image;